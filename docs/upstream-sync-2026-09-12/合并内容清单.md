# 上游合并内容清单（2026-09-12）

> 目标仓库：fork `LiTerBo/GoModel` ← 上游 `ENTERPILOT/GoModel`
> 状态：**待用户逐项确认后执行**（本文档仅为清单，未做任何合并动作）
> 回滚点：`rollback/pre-upstream-sync-2026-09-12` → `f8670bf6`（已推到 fork 远端）
> 上游基线：`071fb8ef` (#955)，即分叉 66 / 落后 65，试合并产生 13 个冲突文件

---

## 1. 合并原则落地为判定标准

原则一「做上游的精简版，只合并高价值部分」→ 高价值判定（满足任一即入清单）：

| 级别 | 判定 | 说明 |
|---|---|---|
| ★★★ 必合 | 协议/格式正确性、计费正确、故障转移与可靠性、安全、依赖与工具链补丁 | 直接影响 fork 已发布版本（v1.x）的真实用户 |
| ★★ 选择性 | 增强既有子系统（护栏、缓存、仪表盘 UX）、小体积文档/测试 | 与 fork 精简目标不冲突时合并 |
| ★ 不合 | 跨切面的新子系统扩张、与 fork 定位无关的通用平台功能、纯演示/宣传物料 | 属"上游平台化"，与精简目标相悖 |

原则二「增强自有的特性功能」→ 每个主题额外标注它如何服务 fork 的四条自有产品线：
能力探测与确认（capability）、复杂度感知路由、定价时间窗覆写、供应商/模型上架开关 + 双语 UI。

---

## 2. 上游 65 提交清单（按主题）

### T1 协议正确性 — 16 提交 · **全部 ★★★ 合并**

| 提交 | 内容 |
|---|---|
| 071fb8ef (#955) | responses 截断上报 + 翻译输出形状归一化 |
| ac672adb (#961) | 后台 responses 补全 + lifecycle 路由去掉模型校验 |
| fbe39112 (#953) | 护栏改写流时重编 sequence number |
| eb2aedd3 (#952) | chat 翻译时丢弃重放的 output item id |
| 957bd868 (#950) | 链式流式响应标注 predecessor |
| 9dc35339 (#945) | 存储流式响应，支持 previous_response_id 续链 |
| 753161bc (#939) | chat 翻译型供应商支持 previous_response_id |
| 40fb07ff (#935) | 翻译流归一化为 OpenAI 事件生命周期 |
| 8a2d22f2 (#930) | Gemini 流式文本轮次签名带回 message item |
| 64c755d0 (#928) | Anthropic thinking 签名回传客户端 |
| 1f9ddfa0 (#929) | 流式转换器共享 thinking 重放状态 |
| 6d042df7 (#964) | thinking block 始终渲染签名 |
| 1ff820b0 (#933) | Anthropic 精确 token 计数 + 估算校准 |
| 13084c32 (#948) | 链路历史防护、流式工具参数还原、增长匹配保持 |
| 8109685b (#944) | transform 流中重写 tool-call 参数 |
| 18eb92e7 (#940) | 审计截断后仍能识别 body 会话信号 |

### T2 供应商适配 — 7 提交 · **全部 ★★★ 合并**

| 提交 | 内容 |
|---|---|
| 2b5fc18c (#973) | 适配供应商不友好参数 + Groq reasoning 归一化 |
| 4b915054 (#954) | 去 xAI 不支持的 reasoning effort、解析 Groq thinking、剥离 x_groq |
| 2cace9ac (#968) | 未知音频参数透传给供应商 |
| 9c021abb (#946) | 按供应商映射 reasoning effort、错误中带供应商实例名 |
| 4a8785fb (#942) | 路由失败错误包含供应商名 |
| 49a836ed (#895) | 供应商重放状态挂在 extra_content 下 |
| 56f72bc0 (#902) | 重启后保留供应商上报的模型元数据 |

> 与自有特性协同：供应商页"登记/上架"开关、模型页能力图标都依赖 provider 元数据完整性（#902 直接相关）。

### T3 可靠性 / 成本 / 可观测 / API — 8 提交 · **全部 ★★★ 合并（#896 需适配）**

| 提交 | 内容 | 备注 |
|---|---|---|
| 638231ff (#949) | 供应商返回 200 但无 choices 时故障转移 | 与 fork 能力探测链路直接相关 |
| 1d4ae811 (#951) | 标记并计数"空 200"响应 | |
| e99d2c3f (#921) | 客户端停止读取时断开连接 | 防连接泄漏 |
| 4471cb92 (#970) | 媒体调用按 1 单位计费（原为 2 或 0） | 计费正确性 |
| f70bb91b (#959) | 新增 GET /v1/models/{model} | OpenAI 兼容补齐 |
| f0b6ce50 (#924) | opt-in GET /v1/auth/verify | |
| af9e91fd (#896) | 状态策略 + 按模型熔断配置 | **与 fork 复杂度路由/virtualmodels 改动重叠，需适配** |
| ca853502 (#899) | 每次配置重载触发 ReloadConfig 钩子 | **增强自有特性：capability 启动回放可在热重载时复用** |

### T4 响应缓存 — 4 提交 · **合并**

| 提交 | 内容 |
|---|---|
| 0d34b58c (#971) | 精确缓存条目按护栏链隔离 |
| 28e6447e (#962) | /v1/embeddings 走响应缓存 |
| 189cb7bf (#936) | 缓存流中出现停滞/消失客户端时正确归类 |
| e7b52c0c (#960) | 文档 cache_mode 与未缓存故障转移 |

### T5 插件与护栏 — 11 提交 · **全部合入（Q3=C：合并引擎 + 配置级精简）**

| 提交 | 内容 |
|---|---|
| 71ef6904 (#885) | 插件系统：护栏阶段、流式钩子、路由策略 |
| 2c0f6623 (#906) | Plugins & Guardrails 页 + 护栏 manifest 开关 |
| 489457de (#938) | 插件契约扩展到 sidecar 集成 |
| 387f732e (#943) | pluginapi 共享配置读取/执行 + plugintest 套件 |
| aeb79070 (#941) | 新增 presidio 护栏（外部服务依赖） |
| f0a7bf45 (#969) | llm_judge 在推理模型上仍生效 |
| 587b1de9 (#917) | 每次提示词护栏编辑记为独立修订 |
| 8617c3ec (#909) | 修订记录保存生效的请求体 |
| 6e6a631a (#922) | 记录每一步护栏结果 + 工作流图着色 |
| 1281524e (#915) | 去掉卡片护栏列表、同步骤护栏堆叠 |
| 3b299059 (#908) | 从工作流图展开护栏步骤 |

> 关键事实（已核实）：上游护栏引擎已重构为**依赖 pluginapi**（`internal/guardrails/*` 14 个文件 import pluginapi），插件引擎是护栏的后端骨架；fork 现有护栏（29 文件）仍是旧实现，且 fork 的仪表盘已有 guardrails 页与装配点。
> 因此"只取护栏审计、不取插件引擎"不可行；T5 整体取舍，用户已裁决 Q3=C：合并引擎，presidio 等外部依赖插件默认关闭、不推广。

### T6 仪表盘 / 多语言 — 6 提交 · **部分合并**

| 提交 | 内容 | 建议 |
|---|---|---|
| e5fea357 (#918) | 脏表单关闭前二次确认 | 合并（与 fork EditorDialog 改动同文件，需融合） |
| 6cbf85a1 (#927) | 限流删除二次确认 | 合并 |
| db499655 (#919) | 工作流卡片/编辑器瘦身 | 合并 |
| a8edd855 (#890) | 供应商卡片链接到文档页 | 合并 |
| 321db77c (#913) | 简体中文 zh-CN 全量译文（1473 key） | **合并，但需与 fork zh.json（1491 key）归并 → Q2** |
| e4beebe5 (#889) | 德语文案 | **排除**（Q1/精简原则，见 §4 文件级操作） |

### T7 基础设施 / 依赖 / 文档 / 演示 — 13 提交 · **以合并依赖为主**

| 提交 | 内容 | 建议 |
|---|---|---|
| cc44cb6c (#914) | Go 1.27.0 → 1.27.1 | 合并（fork go.mod 停在 09-05 基线，一次未升） |
| cc39da9b (#912) | gomod 组 6 项依赖升级 | 合并 |
| c53a3121 (#910) / 36ebb436 (#911) | Docker/golang、QEMU action 升级 | 合并 |
| c987a2ca | 补充缺失测试 | 合并 |
| 87b4d0b0 | e2e 改用 deepseek-flash 并断言 reasoning 映射 | 合并 |
| a5cbe1de (#932) | 发布前 quick wins | 合并 |
| 18474d74 (#926) / 01cd34a6 (#907) | AGENTS 文档规范 | 合并（**保留 fork 的后端英文错误条款**，AGENTS.md 冲突点） |
| c055b7f0 / 7987e7b1 | README 功能清单、About Why-Go 页 | 选择性（与精简定位无关，可省） |
| 88b1f5d9 (#920) | Helm chart 重写 | **排除 helm/ 子树**（Q4=B），同提交内的 storage/usage 代码修复保留 |
| 90a27c15 (#923) | 演示数据播种（工作流/护栏/流量） | **保留**（仅 Makefile + shell 脚本，无运行时影响） |

---

## 3. Fork 侧需同步调整的清单（原则二：增强自有特性）

| # | 动作 | 依据 | 影响面 |
|---|---|---|---|
| F1 | capability 确认结论在热重载后保持：**无需接线**——Reload 重建 App → phases 重跑 → `initCapabilities` 自动重放；已补跨代回归守卫 | #899 | internal/app（测试 + 注释） |
| F2 | ✅ 复杂度路由评分纳入健康/熔断状态：`ext.HealthOracle` 契约 + `health.Tracker.Unhealthy` 信号 + selector 健康过滤（池空则 fail-open） | #896 + #951/#949 | ext/route.go、providers/health、extensions/complexity、app 装配 |
| F3 | 能力探测的 modeldata 与"供应商上报元数据持久化"对齐 | #902 | internal/modeldata |
| F4 | 定价时间窗覆写与上游 pricingoverrides 变更回归（自动合并≠语义正确） | T4/T2 | pricingoverrides + dashboard |
| F5 | 供应商"登记/上架"开关适配上游 provider 参数/错误文案变化 | T2 | providers、admin |
| F6 | 双语 UI 目录归并：zh 命名与去留统一（Q2）、pl 去留（Q1） | #913 | messages/*.json、inlang settings |
| F7 | 扩展点路线统一：fork `ext/route_content.go` 与上游插件路由策略的取舍 | #885 | ext/、internal/extensions |
| F8 | 13 个冲突文件逐一解法（见 §5） | — | — |

---

## 4. 本轮排除清单（文件级 · 已按 Q1–Q4 裁决落定）

机制（Q5=A）：先全量 merge 上游 main，再用**独立 revert 提交**剔除下表内容；**必须文件级而非提交级**——上游提交常混装。

| 排除对象 | 上游来源 | 文件级操作 | 备注 |
|---|---|---|---|
| Helm chart 重写 | 88b1f5d9 (#920) 的 `helm/` 子树（14 文件） | 仅 revert `helm/` 子树，回到 fork 现有 chart | ⚠️ **禁止整提交 revert**：#920 同时含真实修复（`internal/storage/sqlx/{schema,postgres,conformance_test}.go`、`internal/usage/store_postgresql.go`、`internal/usage/pricing_timewindow_test.go`），必须保留 |
| 德语 | e4beebe5 (#889) | 删 `messages/de.json` + 还原 `project.inlang/settings.json` 的 de 项 + 去掉 `tests/i18n.test.js` 的 de 断言 | i18n.test.js 与 Q2 的 zh 改动同文件，一次改完 |
| 波兰语 | 上游仍维护，fork 已删（Q1=A） | merge 时 `messages/pl.json` 取"保留删除"（DU 冲突解法） | 合并后不应再出现 pl.json |
| presidio 护栏 | aeb79070 (#941) | **零代码改动**：presidio 是 opt-in 护栏类型，只有配置中显式写 `type: presidio` 才生效 → ①不写默认配置 ②文档不推广 | 无需 revert |
| demo 数据播种 | 90a27c15 (#923) | **保留**（仅 Makefile + `tools/seed-demo-data.sh`，无运行时影响；剔除反而引入 Makefile 冲突） | 不进排除清单 |
| README/About 宣传文档 | c055b7f0 / 7987e7b1 | **保留**（体积小、无冲突） | 不进排除清单 |
| 插件生态未来扩张 | 后续上游提交 | 建立"不入清单"规则，每轮同步前评估 | 制度化，见 §10 |

> 原则：精简体现在"叶子节点 + 不启用"，而非砍掉上游骨架。跨切面子系统（插件引擎/护栏重构）剥离成本远高于收益，故 Q3 取"合并引擎 + 配置级精简"。

---

## 5. 冲突清单与解法（试合并实测 13 文件）

| 文件 | 类型 | 解法 |
|---|---|---|
| web/dashboard/messages/pl.json | DU（fork 删除/上游修改） | 保留删除（Q1=A） |
| web/dashboard/messages/en.json | UU×1 | 并集：fork 独有 119 + 上游独有 101 + 共有 1372（实测，见 §10） |
| web/dashboard/project.inlang/settings.json | UU×1 | 语言列表定稿＝`en` + `zh-CN`（去 pl、去 de） |
| web/dashboard/tests/i18n.test.js | UU×1 | 按 en/zh-CN 两语言集合改写，并移除 de 断言（Q1/Q2/§4） |
| internal/virtualmodels/balancer.go | UU×2 | 融合 fork 复杂度分带 + 上游状态策略/熔断 |
| internal/virtualmodels/resolve.go | UU×1 | 同上 |
| internal/virtualmodels/sticky_test.go | UU×1 | 用例合并 |
| internal/app/init_admin.go | UU×1 | 装配点：fork capability/modeldata 端点 + 上游 plugins 端点 |
| internal/app/subsystems.go | UU×1 | 同上 |
| web/dashboard/src/lib/components/organisms/EditorDialog.svelte | UU×2 | fork 本地化 + 上游脏表单确认，二者兼容 |
| web/dashboard/src/lib/components/atoms/EnabledToggle.svelte | UU×1 | fork 图标化开关 + 上游样式 |
| web/dashboard/tests/models-virtual-models.test.js | UU×1 | 用例合并 |
| AGENTS.md | UU×1 | 保留 fork 的"后端错误永英文"条款 + 采用上游 AGENTS 规范 |

---

## 6. 决策记录（2026-09-12 已由用户裁决）

| 决策点 | 结论 | 落地影响 |
|---|---|---|
| **Q1 波兰语** | **A 维持 fork 删除** | merge 时 `messages/pl.json` 取"保留删除"；不再维护 pl |
| **Q2 简体中文归属** | **A 采用上游 `zh-CN.json` 命名**，并入 fork 独有 key 与自有 UI 文案，删 `zh.json` | 命名统一为 `zh-CN`；译文取值规则见下方 Q2b（已裁决） |
| **Q3 插件与护栏** | **C 合并插件引擎与护栏重构**，presidio 等外部依赖插件默认关闭 | T5 全部 11 提交合入；presidio 零代码改动（opt-in 类型，见 §4） |
| **Q4 Helm chart** | **B 不合** | 仅 revert `helm/` 子树；#920 混装的代码修复必须保留（见 §4） |
| **Q5 执行机制** | **A 全量合并 + 显式 revert 排除清单** | 本轮及后续每轮同步统一按此办理，排除清单见 §4 |

### Q2b 全文取舍规则（2026-09-12 已裁决）

> 用户裁决：**上游译文只用于上游新增的 101 条，其他不变。**

合并后的 `zh-CN.json` 以上游 `zh-CN.json` 为骨架，译文取值分两类：

| 类型 | key 数 | 译文取值 |
|---|---|---|
| 上游独有 | 101 | **采用上游译文**（本次唯一采纳上游译文的部分） |
| fork 独有 | 119 | 保留 fork 现有译文 |
| 共有（逐字相同 792 / 不同 580） | 1372 | **一律保留 fork 现有译文**（含 580 条与上游译法不同的，不对齐上游写法） |

- **合并后规模**：`en.json` / `zh-CN.json` 各 **1592 key** = 119 + 101 + 1372（en 取双方 key 并集；zh-CN 按上表取值）
- **执行口径**：`zh-CN.json := 上游 zh-CN.json`，再把 fork `zh.json` 中**每一个存在的 key** 覆盖进去（覆盖范围 1372 + 119 = 1491）；`en.json := 双方 key 并集`（缺哪个补哪个）
- **副作用（已知并接受）**：上游后续修改英文文案时，若该 key 在 fork 保留集合内，需人工决定是否同步中文；`tests/i18n.test.js` 的 parity 用例只校 key 集合一致性，不校验译文内容

---

## 7. 执行机制方案（**已定：方案 A**）

| 方案 | 做法 | 代价 | 结论 |
|---|---|---|---|
| **A 全量合并 + 显式 revert 排除清单**（推荐） | 在 `sync/upstream-2026-09-12` 分支 merge 上游 main，再用独立 revert 提交剔除 §4 项；以后每轮同步照此办理 | 需维护一份排除清单；排除项被上游再次改动时产生针对性冲突（数量可控） | 上游 65 提交/6 天的节奏下，唯一可持续的机制；协议正确性自动跟进，历史可审计 |
| B 主题摘取（cherry-pick 主题包） | 只挑 T1–T4/T6 的提交区间打入 fork | 主题内部提交强依赖（如 #961 依赖 #945/#959 等），逐个摘取冲突量大且易漏；每轮重新做一遍 | 仅适合"一次性借鉴"，不适合长期跟车 |
| C 混合（本轮摘取 + 季度跟车窗口） | 本轮用 B 保持仓库最小，季度再做一次 A | 两套机制的维护成本叠加 | 备选 |

---

## 8. 执行记录（2026-09-12 实际执行 · 已完成）

**机制修正（有实测证据）**：原计划的「按主题分批合并」在执行时改为**一次全量合并 + 分阶段验证**。证据：批次① 只合并 9 个提交时冲突文件已达 10 个，而全量 65 个提交合并仅 13 个冲突文件 —— 前缀合并会重复解析同一批文件（en.json / settings.json / virtualmodels / i18n 用例），成本更高。该修正同时更严格地满足 Q5=A（全量合并 + 文件级 revert 排除清单）。

| 步骤 | 结果 |
|---|---|
| 1 计划文档提交 main 并推送 fork | `c3f8973d` ✓ |
| 2 建同步分支 `sync/upstream-2026-09-12` | ✓（main 未动） |
| 3 基线门禁（未合并） | `go build` ✅；Go **87 包 ok / 3973 PASS / 0 FAIL**（静态测试函数 3978） |
| 4 `git merge upstream/main`（071fb8ef） | 13 个冲突文件全部解析（含 1 个 delete/modify：pl.json） |
| 5 排除清单落地 + 回读校验 | `helm/` 与 fork 基线 **0 差异**；`de.json`/`pl.json` 不存在；presidio 未进任何默认配置 |
| 6 i18n 合并（Q2b） | `en` / `zh-CN` 各 **1579 key**，key 集合与顺序一致；剔除 13 个源码已无引用的 key |
| 7 门禁（合并后） | `go build`/`vet` ✅；Go **97 包 ok / 0 FAIL**；仪表盘 **752/752 PASS**（首轮 695 中 7 个失败已定位修复）；`vite build` ✅ |
| 8 真实二进制冒烟 | `/health` 200；`/v1/auth/verify` 200 `{"valid":true,"method":"master_key"}`（上游 #924 生效）；无 key → 401；`/v1/models` → 503「model registry has no models」——**未合并基线同样 503**，属未配置供应商时的既有行为，非本次合并引入 |
| 9 合并提交 | `a5b9fc1e`（parents `c3f8973d` + `071fb8ef`），已推送 fork `sync/upstream-2026-09-12` |

**冲突解析要点**：虚拟模型路由 = 上游 plugin 策略与 fork 自适应选择器并入同一 switch，并统一保留 `ctx`；`internal/app` 装配 = capability 与 routeStrategies 子系统并存；`AGENTS.md` = 上游新结构 + fork「后端错误永英文」条款回插；仪表盘组件 = fork 本地化 aria-label + 上游 `requestClose`/`onToggle` 行为；i18n 用例 = 上游用例的中文预期按 Q2b 保留值修正、语言集去掉 de/pl。另修复一处静默重复 `import`（`EditorDialog.svelte`）——只有 `vite build` 能暴露，`node --test` 不编译组件。

---

## 9. 风险

| 风险 | 缓解 |
|---|---|
| 上游护栏换骨导致 fork 现网护栏行为变化 | T5 单独批次 + 工作流/护栏页回归实测 |
| 13 冲突中 virtualmodels 融合引入路由退化 | 批次③ 后跑虚拟模型/路由全部用例 + 熔断状态用例 |
| zh 目录归并造成文案回退（语言一致性） | i18n parity 用例 + fork 多出的 18 key 逐条核对 |
| 合入后体积增长（上游 +66.5k 行）与"精简"目标背离 | 排除清单制度化，每轮评估；用 `make build` 产物体积对比 |


---

## 10. 附录

### 10.1 i18n 实测数据（key 级，非估算）

| 指标 | 数值 |
|---|---|
| fork `en.json` / `zh.json` | 1491 / 1491 key（集合一致） |
| 上游 `en.json` / `zh-CN.json` / `de.json` | 1473 / 1473 / 1473 key |
| 仅 fork 存在 | **119 key**（capability 图标与面板、时间窗编辑器、供应商刷新/上架、分组折叠、auth key 删除等自有功能文案） |
| 仅上游存在 | **101 key**（护栏流程/步骤状态、工作流帮助、插件页等上游新功能文案） |
| 共有 key | **1372**，其中 zh 译文逐字相同 **792**、不同 **580** |

> 早期估算的"fork 多 18 key"来自 key 总数相减，不成立；以本节实测为准。**Q2b 已裁决（2026-09-12）：仅采用上游新增 101 条的译文，其余 1491 个 key 保留 fork 译文，见 §6。**

### 10.2 每轮同步的标准作业（Q5=A 制度化）

1. `git fetch upstream main` → 记录 `rev-list --count origin/main..upstream/main` 与本轮 merge-base
2. 建 `sync/upstream-<日期>` 分支，先打回滚 tag（`rollback/pre-upstream-sync-<日期>`）并推送
3. 试合并（worktree 内 `git merge --no-commit`）→ 记录冲突文件与 hunk 数
4. 全量合并；随后按 §4 排除清单实施文件级 revert（禁止整提交 revert）
5. 协议/供应商/可靠性批次优先跑门禁；再处理仪表盘与 i18n
6. 门禁：`make build` / `make test`（已知基线失败：`tests/perf`）/ 仪表盘 `npm test` + i18n parity
7. 回归 fork 六项自有特性（能力探测确认、能力图标、复杂度路由、时间窗定价、供应商双开关、分组折叠）
8. 通过后合并回 main 并打 tag；失败 `git reset --hard rollback/pre-upstream-sync-<日期>`
9. 每轮结束时更新本文档 §2/§4（新增上游提交的价值判定与排除项）

---

## 11. 剩余工作（下一步，等指示）

| # | 项目 | 说明 |
|---|---|---|
| ✅ N1 | F1 capability 热重载后保持确认结论 | **结论：无需 `ReloadConfig` 接线**——reload 重建 App 即自动重放；已加跨代测试 `TestCapabilityConfirmationsSurviveRebuild`（见 §12） |
| ✅ N2 | F2 复杂度路由纳入健康/熔断评分 | 已接线：`ext.HealthOracle` 契约 + `Tracker.Unhealthy` 信号 + selector 健康过滤（fail-open）+ `attachRouteHealth` 装配（见 §12） |
| ✅ N3 | F3 modeldata 与「供应商上报元数据持久化」对齐 | **两处溯源缺口均已修**：无目录上报能力 → `discovered`（方案 A）；配置声明能力 → `config`（方案 A′）。均有红→绿用例；一处既有 no-op 用例随语义更新（见 §13） |
| ✅ N4 | F4 时间窗定价语义回归 + 页面实测 | **两项完成**：接口级实测（真二进制 + mock 上游；成本 3.0 vs 30.0 作判伪点）+ 页面级修复 F4-UI-1（行内定价状态漏替 `time_windows`）（见 §14） |
| ✅ N5 | F5 供应商「登记/上架」双开关在上游 providers 变化下的实测 | **实测通过**（mock 上游 + admin API）：下架→400 `model_access_denied`、未登记→503 空 inventory、两者均标可逆；`/admin/models` 的 `default_enabled`/`effective_enabled` 字段对支撑 #31（见 §15） |
| ✅ N6 | 冒烟补全：配置真实供应商后验证 `/v1/models` 与路由链路 | **实测通过**：API 登记供应商 → 发现 → `/v1/models` 列出 → chat 落到 mock 上游返回 usage（见 §15） |
| ✅ N7 | 回归清单：fork 六项自有特性逐项实测 | **收口**：时间窗定价 ✅（§14）、双开关 ✅（§15）、复杂度路由 ✅（§16，10/10 断言，含健康绕行与 fail-open）、能力探测确认 ✅（§12 F1，两代 App 共享 SQLite 自动重放）、能力图标 ✅（9-11 轮三处修复 + §13 溯源口径）、分组折叠 ✅（#14 已收口） |
| ✅ N8 | 合并回 main + 打版本标签 | **已完成**：main `c3f8973d` → `c58bb9fc`（ff，上游并入边界＝分支内合并提交 `a5b9fc1e`）；发布门禁在 main 重跑全绿（Go exit=0/97 包、前端 754/754、vite ✓）；annotated tag **`v1.2.0`** 已推远端（见 §17） |

---

## 12. 第二轮执行记录：自有特性接线（F1 + F2）

### F1 — capability 热重载后保持确认结论（结论：**无需接线**）

原计划假设要把 capability 服务挂到 #899 新增的 `run.Options.ReloadConfig` 钩子上。读码 + 跨代实测证明该假设不成立：

| 事实 | 依据 |
|---|---|
| `ReloadConfig` 是**发行版钩子**，只在"已存在前一版配置"时调用；本仓 `cmd/gomodel` 未注册（全仓 `grep ReloadConfig` 仅命中 `run/run.go` 的定义与调用处） | run/run.go |
| 每次配置重载 `run.Run` 通过 `rebuild` **重建整个 App**（新 generation） | run/run.go |
| App 构造按 `bootstrap.phases()` 顺序执行，其中 `initCapabilities` → `capability.New` → `service.Refresh` 重放持久化确认 | bootstrap.go / init_capability.go / capability/factory.go |

所以"热重载重放"由架构自动满足，真正的风险是未来重构把 `initCapabilities` 移出或移到 `initProviders` 之前。已补两处守卫：

- 行为测试 `internal/app/capability_reload_test.go`：两个 generation 共享同一 SQLite 文件、第二代是全新 registry，全程只调用一次 `Confirm`；第二代启动日志 `capability confirmations loaded into registry count=1`。
- `initCapabilities` 注释写明"每个 generation 都会执行、必须排在 initProviders 之后"。

`ReloadConfig` 保持空置：它服务于发行版自定义策略，当前无使用场景。

### F2 — 复杂度路由纳入健康/熔断状态（原则二"增强自有特性"）

信号 → 决策 → 装配三段：

| 段 | 落地 |
|---|---|
| 信号 | `providers/health/tracker.go` 新增 `Tracker.Unhealthy(provider, model) bool`：provider 熔断 open（provider 级）或该 provider/model 命中窗口错误阈值（模型级，复用 `flaggedModel`）。无流量目标、未知 provider 一律 healthy；nil 接收者安全 |
| 决策 | `extensions/complexity/engine.go` 在**能力硬过滤之后**加健康过滤：剔除 unhealthy 候选；过滤后池空则 **fail-open** 用原池（否则等于放弃选择，把请求交回 round-robin 打同一批坏目标），由档位偏好继续决策 |
| 装配 | `app/init_foundation.go` 新增 `attachRouteHealth`：selector 实现 `ext.HealthAwareSelector` 时注入 tracker；不支持或 oracle 为 nil 则跳过，老 selector 零改动 |

契约放在 `ext/route.go`（叶子包，无 internal 依赖）：`HealthOracle` + `HealthAwareSelector`，支持构造后换源。

测试：新增 4 个测试文件、11 个顶层测试（共 23 条可判伪用例：健康信号 11、路由决策 7、装配 4、跨代重放 1），`go test ./internal/providers/health/... ./internal/extensions/... ./internal/app/...` 全绿。

---

## 13. F3 评估结论（N3 · 待决策，代码未改）

### 结论：6 条语义中 5 条已对齐，1 条实测缺口

新增测试 `internal/app/capability_metadata_alignment_test.go`（两代：活体上报 → 缓存落盘 → 重启且供应商不可达 → 能力重放）实测：

| # | 语义 | 实测 |
|---|---|---|
| 1 | #902：供应商上报的 `context_window`/`max_output_tokens`/`modes` 在**重启且供应商不可达**后保留 | ✅ 32000/4096/[chat] 全保留 |
| 2 | 供应商上报的具备能力值保留 | ✅ `function_calling=true` |
| 3 | fork 确认结论仍**胜出**恢复的上报值（上报 vision=true vs 确认 vision=false） | ✅ 发布态 false，source=`test` |
| 4 | 确认覆写**不写入**模型缓存（否则会绕过能力存储"复活"陈旧结论） | ✅ 缓存内 vision 仍为 true 且 source≠test |
| 5 | 目录（model list）存在时上报能力带 `discovered` 溯源 | ✅ source=`discovered` |
| 6 | **无目录时上报能力带溯源** | ❌ **`capability_sources` 为空** |

缺口定位（实测 A/B 对比，非推断）：`discovered` 打标逻辑藏在目录富化函数里（`internal/modeldata/enricher.go:61-67`，由 `registry_cache.go:118` 的 `if list != nil` 与活体路径的同构条件触发）。因此**没有配置 model list 的部署**（合法配置）在活体拉取与缓存恢复两条路径上都拿不到溯源，与 `docs/capability-routing/架构设计.md:22` 声明的 `CapSrcDiscovered` 意图不符。#902 让该缺口在重启后更常被看见（此前这些能力干脆消失）。

影响面：`/v1/models` 输出"值存在但无从解释"，第三方客户端无法判断来源；仪表盘能力面板按 source 分类时会落入"未知来源"。

### 待决策方案（编号）

| 方案 | 做法 | 代价 | 评价 |
|---|---|---|---|
| **A（推荐）** | 在 `newModelInfo`（`internal/providers/registry.go:39`）为入参元数据里的能力键补 `CapabilitySources[k]=discovered`（仅当未标注），后续 config/registry/heuristic/test/observed 层照旧覆盖 | 改动落在上游核心文件（~10 行）；配置模型路径（`configured_models.go:222` 同走此构造函数）在 config 覆写生效前会短暂标 discovered，须补该路径测试 | 根因位置最准：构造点就是"供应商上报"语义的边界 |
| B | 在 fork 自有 `internal/modeldata` 增回填步骤：值等于 `info.Discovered` 且无来源 → 标 discovered，置于目录富化之后 | 多一次遍历；值相等无法区分"配置值与上报值恰好相同"（无害） | 零上游文件冲突面，适合想保持精简同步代价 |
| C | 后端不改，仪表盘把"有值无来源"渲染为"供应商上报（未标注）"+ 文档写明语义 | 机器可读性仍弱 | 最低成本，治不了 `/v1/models` |
| D | 登记为本轮已知偏差，不改 | 缺口保留 | 不推荐（与架构设计文档冲突） |

> 状态：第 1–5 项已由 `internal/app/capability_metadata_alignment_test.go` 固化（含「有目录时 discovered 溯源」正例与「确认结论不泄漏进缓存」反例）；第 6 项已按**方案 A** 修复（见下）。

### 实施记录（方案 A · 已实施）

| 项 | 内容 |
|---|---|
| 实现 | `internal/providers/registry.go` 的 `newModelInfo` 增加 `stampDiscoveredCapabilitySources`：为供应商上报的每个能力键补 `core.CapSrcDiscovered`（仅当该键尚无来源；先 `Clone()` 再打标，不改调用方响应对象）。`Discovered` 与发布态一并带上溯源，故缓存往返后依旧可读。位置选在"上报→注册表"的边界而非目录富化：无目录部署根本不跑富化 |
| 红→绿 | 新增 `TestProviderReportedCapabilitiesCarryDiscoveredProvenance`（活体拉取 + 缓存恢复两条路径）先红（4 条 `source = ""`），实现后转绿 |
| 层次优先级 | 未被破坏：`config/test/observed` 覆写仍胜出（同文件既有断言 `vision=false` + source=`test` 保持绿）；有目录时打标幂等（`TestDiscoveredProvenanceNeedsACatalog` 绿） |
| 前端语义 | `web/dashboard/src/pages/models/capabilityIcons.js` 只把 `test`/`observed` 当"实测确认"，`discovered` 与 registry/config/heuristic 同属"声明态"→ **图标状态不变**，变化的只是溯源可读（`/v1/models` 与 tooltip 不再缺来源） |
| 门禁 | `go test ./cmd/... ./config/... ./ext/... ./internal/... ./run/...` → test_exit=0，97 包 ok，0 FAIL |

### 新发现 F3-2（同族缺口 · 待决策，未改）

`CapSrcConfig` 在 `internal/core/types.go:335` 定义但**生产代码从未使用**：config.yaml 声明的能力（`provider_models[].metadata.capabilities`）经覆写通道进入模型后**没有来源标注**。对称面证明这是遗漏而非设计——**定价**在覆写通道里显式打标（`registry_metadata.go:380` 用 `FieldSources(core.ModelPricingSourceConfigYAML)`），能力没有对应动作。

| 方案 | 做法 | 代价 |
|---|---|---|
| **A′（推荐）** | 在 `applyConfigMetadataOverrides`（`registry_metadata.go:377` 附近）为覆写贡献的能力键补 `CapSrcConfig`，与同函数定价的做法对齐（~6 行 + 测试） | 动一处上游函数；需明确"覆写贡献的键"＝`override.Capabilities` 的键 |
| B′ | 只在文档写明"配置声明的能力无来源"，不改代码 | `/v1/models` 仍无法区分"配置声明"与"未知来源" |
| C′ | 登记为已知偏差，与下一轮同步一并处理 | 缺口保留 |

> **已按方案 A′ 实施**（记录见下）。

### 实施记录（方案 A′ · 已实施）

| 项 | 内容 |
|---|---|
| 实现 | `applyConfigMetadataOverrides` 为覆写贡献的能力键补 `core.CapSrcConfig`；新增 `configCapabilitySources`（`registry_capabilities.go`，跳过覆写自带来源的键），复用定价分支同一个 source-map 合并函数 → 与 `FieldSources(ModelPricingSourceConfigYAML)` 完全对称 |
| 语义 | 覆写声明的键＝`config`；覆写自带来源的键（确认通道 `test`/`observed`）保持不变；未涉及的键保留原来源（如 `discovered`）。幂等：二次应用内容相同，不产生替换 |
| 红→绿 | 新增 `internal/providers/registry_capability_sources_test.go`：`TestConfigMetadataOverrides_StampCapabilitySources` 先红（`vision source = ""` → `config`）；`TestConfigMetadataOverrides_KeepSourcedKeysAsDeclared` 全程绿（证明确认来源不被改写） |
| ⚠️ 既有用例语义更新 | `TestApplyConfigMetadataOverrides_NoOpPreservesPointerIdentity` 随语义更新夹具：溯源纳入比较后，"逐字段值相同"不再等于"无变化"（新增来源即变化）。夹具改为**已带 `config` 来源**的既有模型，用例继续守"真无变化时指针稳定"，同时证明新打标幂等 |
| 前端 | 复用既有语义：`capabilityIcons.js` 只把 `test`/`observed` 当"实测确认"，`config` 属"声明态" → 图标与 tooltip 不变，仅 `/v1/models` 能说明来源 |
| 门禁 | `go test ./cmd/... ./config/... ./ext/... ./internal/... ./run/...` → test_exit=0，97 包 ok，0 FAIL |

---

## 14. N4（F4）时间窗定价回归 + 接口实测（已完成）

### 背景修正

时间窗定价是**上游特性**（`0df87bb8` 把 time-of-day 窗口应用到成本计算、`f852629b` 让 API 支持 `time_windows`）；fork 在此之上做的是**仪表盘编辑器**（`docs/time-window-editor/`）。本轮同步还合入上游 `4471cb92`（#970，媒体计费改为按一个单位）——它改了 `internal/usage/cost.go`、`pricingoverrides/types.go`、`validation.go`（新增 `output_image_per_mtok`），正是同一片区域，即 F4 的风险面。

### 接口级实测（真二进制 + mock 上游，非单测）

配方：mock OpenAI 兼容上游（报告 1 个模型；chat 返回 1,000,000 input + 1,000,000 output tokens）→ 真 `cmd/gomodel`（sqlite 存储）→ 用**仪表盘同款 admin API** `PUT /admin/model-pricing-overrides` 写覆写 → 走 `/v1/chat/completions` → 从 `/admin/usage/log` 读回成本（脚本：`/tmp/f4/{mock_upstream.py,harness.py}` + `/tmp/f4/config/config.yaml`）。

| # | 步骤 | 实测结果 |
|---|---|---|
| 1 | 模型发现（mock `/v1/models`） | `local/f4-offpeak-model` ✅ |
| 2-3 | 覆写：基础价 10/20 + 时间窗 00:00–23:59（窗内价 1/2） | 200；往返保留 `time_windows` 全字段（label/utc_ranges/pricing）✅ |
| 4-5 | 发请求（窗**覆盖**当前 UTC 时刻）→ 成本 | **3.0**（1+2，窗内价生效）✅ |
| 6-8 | 改为**不覆盖**当前时刻的窗 → 再发请求 → 成本 | **30.0**（10+20，基础价）✅ ← 判伪点：证明窗口过滤真实有效，而非"有窗就套用" |
| 9-11 | 重启进程后再发请求 | 覆写持久化 ✅、成本仍 30.0 ✅、`cost_source=model_pricing` 可溯源 ✅ |

结论：合并**未**破坏"窗口按请求时刻（UTC）生效"的语义；#970 的媒体计费改动与该语义在单测层共存（`internal/usage/{pricing_timewindow,images,audio}_test.go` 全绿）。**边界**：媒体路径本轮只到单测级，未做接口级实测。

### 页面级发现与修复 F4-UI-1（已修）

`modelRowPricingState`（`web/dashboard/src/pages/models/pricingOverridesLogic.js`）替换标量价与 `tiers`，**漏替 `time_windows`**；而 `docs/time-window-editor/架构设计.md:239-249` 明确要求"override 设置 `time_windows` 时替换掉 metadata 的窗口"。影响：保存覆写后，模型表格的时间窗提示（`timeWindowHint` 消费该 pricing）仍显示**目录/元数据**的窗口 → 显示口径与计费口径不一致（计费按覆写窗口）。

修复：同一处补 `time_windows` 替换 + `sources.time_windows = overrideSource`（与标量/tiers 同构）；新增 2 个前端用例（覆写替换 / 无覆写保留元数据窗口）。门禁：`npm test` **754/754**（+2）、`npm run build` 成功。

---

## 15. N5 + N6 供应商登记/上架与路由链路实测（已完成 · 代码无需改动）

### 配方

同一个 mock 上游（openai 兼容，报告 1 个模型）→ 真 `cmd/gomodel`（sqlite，**config 不内联 `providers`**，供应商完全通过 API 登记）→ 状态切换全部走**仪表盘同款 admin API**：`PUT /admin/provider-credentials`（登记）与 `PUT|DELETE /admin/virtual-models`（上架策略，selector 形态 `<provider>/`）。脚本：`/tmp/f4/n5_n6.py`、`/tmp/f4/dump_states.py`。

### N6 路由链路（登记 → 发现 → 列表 → 调用）

| 步骤 | 实测 |
|---|---|
| `PUT /admin/provider-credentials`（vllm + mock base_url, enabled） | 200；列表回读 `enabled: true` |
| `GET /admin/providers/status` | `status=healthy`、`status_reason=configured and model discovery succeeded` |
| `GET /v1/models` | `local/f4-offpeak-model` ✅ |
| `POST /v1/chat/completions` | 200，实际落到 mock 上游（usage 1e6/1e6 原样返回）✅ |

### N5 上架/下架（provider 级策略）

| 步骤 | 实测 |
|---|---|
| `PUT /admin/virtual-models {source:"local/", enabled:false}` | 200 |
| `GET /v1/models` | **200 + 空列表**（模型移出可用清单） |
| `POST /v1/chat/completions` | **400 `model_access_denied` / "requested model is not available"** ← 干净的拒绝语义，不是 500 |
| `GET /admin/models` | 行仍在：`access = {selector:"local/f4-offpeak-model", default_enabled:true, effective_enabled:false}` ← 正是 #31 需要的字段对（"默认开、被策略关"）；前端 `tests/models-provider-serving-gate.test.js` 据此把叶子开关置灰 |
| `DELETE /admin/virtual-models {source:"local/"}` | 204；`/v1/models` 恢复列出；chat 200 ✅ |

### N5 登记（凭据 enabled）

| 步骤 | 实测 |
|---|---|
| `PUT /admin/provider-credentials ... enabled:false` | 200 |
| `GET /v1/models` | **503** + 空列表（inventory 为空） |
| `POST /v1/chat/completions` | 400 `provider_error: model registry has no models...` |
| `GET /admin/providers/status` | `summary.total=0`、`providers: []`（该供应商整体退出） |
| 重新 `enabled:true` | `/v1/models` 恢复、chat 200 ✅（可逆） |

### 一处对外行为不对称（登记 ≠ 下架，已登记在案、本轮不改码）

同样"不可用"，对客户端表达不同：**下架** = `/v1/models` 200 + 空、调用 400 `model_access_denied`；**未登记** = `/v1/models` **503**、调用 400 `registry has no models`。语义上可解释（未登记＝inventory 里根本没有该供应商；下架＝inventory 有但被策略拦住），且两者都可逆。建议留待后续与上游 providers/registry 语义对齐时复核；若届时要求"对外统一表达"，再评估是否收敛状态码。

---

## 16. N7 复杂度路由接口级实测（已完成 · 10/10 断言 · 代码无需改动）

### 装置

`internal/extensions/complexity` 是 fork 的复杂度感知选择器（`ext.RouteSelector` 参考实现），配置入口 `extensions.complexity_routing`，只在虚拟模型 `strategy: adaptive` 的候选池里挑目标。装置：mock 上游（两个模型 `cheap-model`/`strong-model`，可控故障 + 逐次尝试日志）→ 真 `cmd/gomodel`（HEAD `8f722364` 构建）→ 经 admin API 建 `smart` 虚拟模型（**`failover:false` + `session_affinity:false`**，因此日志里每一行都是**选择器自己的选择**，不是重试兜底）。脚本 `/tmp/f4/n7_complexity.py`、`/tmp/f4/mock_n7.py`。

输入设计（阈值 `0.25/0.6/0.9`）：SIMPLE=单条 12 字消息；COMPLEX=约 4.2k 字 + 围栏代码 + 3 条编号步骤。用同公式的 Python 复刻先算分：SIMPLE **0.1924 → simple 档**、COMPLEX **0.7091 → complex 档**（实测结果与其一致，说明不是"碰巧分层"）。

### 实测结果

| # | 断言 | 实测 |
|---|---|---|
| A1 | 选择器已安装（面板可提供 adaptive） | `GET /admin/runtime/config` → `VIRTUAL_MODEL_STRATEGIES='round_robin,cost,failover,adaptive'` ✅ |
| A2 | 建 adaptive 虚拟模型 | 200 ✅ |
| A3 | 两个目标已发现 | `/v1/models` → `local/cheap-model, local/strong-model, smart` ✅ |
| A4 | 简单请求 ×2 | 每次尝试都是 `cheap-model`，200 ✅ |
| A5 | 复杂请求 ×2 | 每次尝试都是 `strong-model`，200 ✅ |
| A6 | 健康追踪识别失败目标 | `/admin/providers/status` → `status=degraded`、`status_reason=recent requests are failing for: cheap-model` ✅ |
| A7 | **简单请求绕开失败目标**（F2 端到端） | 尝试 `strong-model`，200 ✅ ← 本轮新接线在真链路上生效 |
| A8 | **全池不可用时 fail-open** | cheap/strong 双双被标记后，仍按档位优先尝试 `cheap-model`（随后 500）✅ ← 不因"全都坏"而拒绝挑选，符合设计注释 |

### 两处测试假设的修正（值得记住的观测口径）

1. 策略清单来自 **`GET /admin/runtime/config`**（`VIRTUAL_MODEL_STRATEGIES`），不是 `/admin/config`（404）。
2. 健康"标记"阈值：**10 分钟窗口内 ≥3 次错误且错误占比 ≥50%**（`flagMinErrors=3`、`errors*2 >= requests`）。所以 A8 必须先把**两个**目标各推到 3 次错误，否则"池非空"只会走到 A7 的效果——这解释了首轮 A8 的 9/10。

---

## 17. N8 合并回 main 与 v1.2.0（已完成）

| 步骤 | 结果 |
|---|---|
| 合并方式 | `git merge --ff-only sync/upstream-2026-09-12`：main `c3f8973d` → **`c58bb9fc`**。上游"一次性并入"的边界由分支内既有的合并提交 **`a5b9fc1e`**（parents `c3f8973d` + `071fb8ef`）保留，因此无需再造一个空合并提交 |
| 发布门禁（在 main 上重跑，`-count=1` 禁缓存） | `go build ./...` **exit=0**；`go test -count=1 ./cmd/... ./config/... ./ext/... ./internal/... ./run/...` **exit=0 · 97 包 ok · 0 FAIL** |
| 前端门禁 | `npm test` **754/754（0 fail、0 skip）**；`npm run build` **✓ built**（`internal/admin/dashboard/static/dist` 已刷新） |
| 版本标签 | annotated **`v1.2.0`**（指向含本节记录的发布提交） |
| 回滚点 | 合并前主干 `f8670bf6` 保留为 `rollback/pre-upstream-sync-2026-09-12`（本地 + 远端） |
| 排除项（确认未并入） | `helm/` 图表；presidio 等外部依赖（默认关）；`de.json`/`pl.json` 语言包（维持删除） |

v1.2.0 构成：上游 `071fb8ef`（65 commits）全量并入 + fork 六项自有特性（能力探测确认、能力来源溯源、复杂度路由含健康绕行、供应商登记/上架双开关、时间窗定价编辑器、模型分组折叠）+ 本轮三处修复（F3 两处溯源缺口、F4 行内定价状态漏替 `time_windows`）。


