# 工作任务清单：能力判定与动态路由体系

> 日期：2026-09-06 ｜ 状态：待评审
> 关联：需求说明.md、架构设计.md（本目录）
> 执行：按阶段顺序 TDD（RED→GREEN→REFACTOR），每阶段完成汇报（测试数、抓出的 bug），待「继续」指令推进下一阶段

---

## 阶段 A：扩展契约（W1 前置）

- [ ] A-1 `ext.RouteRequest` 增 `Content`（轻量摘要）+ `RequiredCapabilities []string` 字段
  - 测试：旧 selector 编译不破坏；新字段零值行为与旧路径一致
- [ ] A-2 `ext.RouteCandidate` 增 `Capabilities map[string]bool` 字段
  - 测试：零值（nil map）不 panic
- [ ] A-3 `virtualmodels/adaptive.go` 组装 Content/能力进 `RouteRequest`，能力深拷贝（沿用 `copyPrice` 模式）
  - 测试：扩展不可篡改共享 catalog 能力（深拷贝断言）

## 阶段 B：复杂度估计器（W1）

- [ ] B-1 `ComplexityEstimator` 纯函数：特征提取（长度/计数/has_code/has_multi_step/tool 数/上下文长度）
  - 测试：各特征单测 + 空 messages 边界 + 代码围栏/多步列表正则命中
- [ ] B-2 复杂度分级（log 归一化 + 阈值 → simple/medium/complex/very_complex）
  - 测试：分级边界（阈值上下）断言

## 阶段 C：决策引擎（W1）

- [ ] C-1 `RouteDecisionEngine`：复杂度 → tier 映射（primary/upgrade/fallback 配置）
  - 测试：各 tier 映射 + 未配置 tier 的 fallback 顺序
- [ ] C-2 能力过滤：vision 硬过滤 + function_calling 软偏好 + tier 内 fallback 最近 capable tier
  - 测试：硬过滤剔除 / 软偏好优先但降级不阻断 / 全不满足 fallback

## 阶段 D：complexity.Selector 扩展（W1）

- [ ] D-1 `complexity.Selector` 实现 `ext.RouteSelector`（`Select` 组合 CE + DE + 能力过滤）
  - 测试：`Select` 返回空/越界时降级 round-robin（fail-open）
- [ ] D-2 注册进 `ext.Registry`（`RegisterRouteSelector`）
  - 测试：未注册 selector 时 adaptive 回退 round-robin（现有语义）

## 阶段 E：请求内容 threading + 能力需求解析（W1）

- [ ] E-1 gateway 能力需求解析：`requires_function_calling`（`req.Tools`）/ `requires_vision`（`image_url` ContentPart）
  - 测试：纯解析函数单测（含 responses 协议的 input_image 变体）
- [ ] E-2 请求内容摘要 thread 进 resolution 链（Prepare → `ResolveRequestModelWithAuthorizer` → `ResolveExecutionSelector` → `ResolveModelForUserPath` → `balancedResolution` → `adaptiveTarget`）
  - 测试：集成测试断言 Select 拿到摘要；`ModelResolver` 接口扩展的兼容性

## 阶段 F：虚拟名接入 + 阈值试运行（W1）

- [ ] F-1 `smart` 虚拟模型 redirect（strategy=adaptive）接入，targets 配置化
  - 测试：`smart` 解析命中 selector；未配置时回退现有解析
- [ ] F-2 阈值/权重配置段（试运行人工经验值）+ config reload 热更新
  - 测试：阈值热更新生效（复用 snapshot 热换）

## 阶段 G：主动探测（W2a ModelTester）

- [ ] G-1 `internal/modeldata/modeltest`：type 探测（chat/embeddings endpoint 探测）
  - 测试：mock 上游各 endpoint 成功/失败 → mode 判定
- [ ] G-2 function_calling 探测（带 tools + 诱导 prompt，看 `tool_calls`）
  - 测试：mock 回 tool_calls / 无 tool_calls / 网络失败 → pass/inconclusive/inconclusive
- [ ] G-3 三态结果模型 + INCONCLUSIVE 永不误杀（negative 不覆盖）语义
  - 测试：inconclusive 不改静态判型能力；并发/最小载荷约束

## 阶段 H：CapSrcTest 来源 + 主动探测固化（W2a）

- [ ] H-1 `core/types.go` 增 `CapSrcTest` 枚举 + `Clone()`/`MergeMetadata` 深拷贝同步
  - 测试：来源序列化 + 合并优先级（config/registry 胜出）
- [ ] H-2 主动探测结果固化：operator 确认 → models.json `capabilities` / config override
  - 测试：固化后 enrichment 管道读到新能力（`CapSrcConfig/Test` 胜出默认推断）
- [ ] H-3 admin API：`POST /admin/models/test`、`GET /admin/models/test-results`、`PUT /admin/models/capabilities`
  - 测试：门禁（master/托管 key）+ 参数校验 + 结果回读

## 阶段 I：被动观测（W2b 审计日志挖掘）

- [ ] I-1 `internal/auditlog` 增轻量字段 `had_tools`/`response_tool_calls`/`had_image` + gateway 采集点打标
  - 测试：字段采集单测（带 tools/无 tools/带图/响应 tool_calls 打标）；不落全文
- [ ] I-2 聚合器：按 `resolved_model` 分组，去重会话计数，≥3 阈值；`had_tools ∧ 上游报错` → 建议不支持；其余 INCONCLUSIVE 不生成建议
  - 测试：聚合逻辑单测（阈值边界、去重、INCONCLUSIVE 语义）
- [ ] I-3 `CapSrcObserved` 固化通道：operator 门控确认 → models.json/config；确认前不改判型
  - 测试：确认前能力不变；确认后 enrichment 读到新能力
- [ ] I-4 dashboard 待确认队列：`CapabilitySuggestion` 展示 + operator 确认/拒绝
  - 测试：确认/拒绝交互 + 状态流转

## 阶段 J：dashboard 视觉区分 + 测试入口（三来源）

- [ ] J-1 `ModelRow`/`ModelsPage` 能力来源徽标（静态判型 / 主动探测 / 被动观测）
  - 测试：svelte 单测渲染三来源徽标；i18n（en/zh）补齐
- [ ] J-2 模型测试触发入口（单模型/供应商全模型/全部，手动触发）
  - 测试：触发调 admin API；结果回填展示

## 阶段 K：运行时错误告警（W3）

- [ ] K-1 类型/能力错误分类（endpoint↔mode 不匹配 + 上游 404/400 信号）
  - 测试：分类函数单测（type_mismatch / capability_mismatch / 普通失败）
- [ ] K-2 错误信号记录（复用审计日志，见 AD-5）
  - 测试：信号落库 + 不含请求正文（脱敏）
- [ ] K-3 模型模块 WARN 图标 + 错误明细
  - 测试：有信号的模型显示 WARN；点击可见明细

## 阶段 L：文档收口

- [ ] L-1 `docs/config-manual.md` 同步新增 `routing`/`model_test`/`capability_observation` 配置段
- [ ] L-2 三件套回写（本清单 checkbox 勾选 + 决策附录同步）
- [ ] L-3 重建 codebase-memory 索引（`index_repository`，改码后手动重建）

---

## 预估工作量

| 阶段 | 内容 | 预估 |
|------|------|------|
| A | 扩展契约 | 0.5 人日 |
| B | 复杂度估计器 | 1 人日 |
| C | 决策引擎 + 能力过滤 | 1 人日 |
| D | Selector 扩展 | 0.5 人日 |
| E | 内容 threading + 能力需求解析 | 1.5 人日 |
| F | 虚拟名 + 阈值试运行 | 1 人日 |
| G | 主动探测（ModelTester） | 2 人日 |
| H | CapSrcTest + 固化 + admin API | 1.5 人日 |
| I | 被动观测（字段 + 聚合器 + 门控固化） | 1.5 人日 |
| J | dashboard 视觉 + 入口 | 1.5 人日 |
| K | 运行时告警 | 1.5 人日 |
| L | 文档收口 | 0.5 人日 |
| **合计** | | **≈ 14 人日** |

> 注：阶段 F 的「审计日志 K-Means 重校准」与阶段 G 的 `vision/audio` 探测为**二期待办**（见架构设计 §8），不在本期工作量内。
