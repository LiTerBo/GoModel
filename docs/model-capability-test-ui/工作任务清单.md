# 模型能力测试 UI — 工作任务清单

> 日期：2026-09-09 ｜ 状态：待评审
> 关联文档：`需求说明.md`，`架构设计.md`
> 背景决策见需求说明 §13（D-1 ~ D-7），架构设计 §6（AD-1 ~ AD-5）

---

## 阶段 A：后端 capability store（SQLite CRUD + 表初始化）

| # | 任务 | 文件 | 验收标准 |
|---|------|------|----------|
| A-1 | [ ] 定义 `Confirmation` 数据模型 + `Store` 接口（含 Upsert/List/Delete/Close） | `internal/capability/store.go` | 接口覆盖所有 CRUD 操作，`Confirmation` 含 Provider/Model/Capability/Source/Value/CreatedAt |
| A-2 | [ ] 实现 SQLite store：表 DDL + Upsert（INSERT OR REPLACE）+ List + Delete | `internal/capability/store_sql.go` | 单元测试验证 CRUD：空表 List 返回空、Upsert 后 List 返回一条、Delete 后消失、重复 Upsert 覆盖 |
| A-3 | [ ] store 测试验证 `source` 字段正确存储与还原 | `internal/capability/store_sql_test.go` | 写入 source="test" 后 List 读出 source == "test"；写入 source="observed" 同理 |

## 阶段 B：后端 capability service（Refresh + Confirm + 后台刷新）

| # | 任务 | 文件 | 验收标准 |
|---|------|------|----------|
| B-1 | [ ] 实现 `ModelRegistry` 接口（适配 `*providers.ModelRegistry.MergeModelCapabilities`） | `internal/capability/service.go` | 接口仅暴露 `MergeModelCapabilities(provider, model, caps, source) bool` |
| B-2 | [ ] 实现 `Service`：`Refresh(ctx)` 从 store.List 读出所有记录，逐条调用 `MergeModelCapabilities` 注入 registry | `internal/capability/service.go` | 空表 Refresh 不报错；有 3 条记录时 Refresh 后 registry 中对应模型的能力已更新；错误的 store 返回错误 |
| B-3 | [ ] 实现 `Service.Confirm(ctx, provider, model, caps, source)`：遍历 caps 逐条 Upsert + MergeModelCapabilities | `internal/capability/service.go` | Confirm 后 store.List 返回写入记录；registry 对应模型能力已更新；caps 空时跳过不报错 |

## 阶段 C：Admin handler 集成（确认写 store + registry + 启动接线）

| # | 任务 | 文件 | 验收标准 |
|---|------|------|----------|
| C-1 | [ ] `Handler` 新增 `capabilities Confirmer` 字段 | `internal/admin/handler.go:60` | 编译通过 |
| C-2 | [ ] `ConfirmModelCapabilities` 改为先调 `h.capabilities.Confirm()`（写 SQLite），再调 `h.modelTest.MergeModelCapabilities()`（注入 registry） | `internal/admin/handler_modeltest.go` | `go test ./internal/admin/...` 通过；Confirm 场景集成测试验证 SQLite + registry 两写 |
| C-3 | [ ] 新增 `initCapabilities()` 在 `initPricing()` 后调用：`capability.New(ctx, appCfg, app.storage, registry)` → 启动加载 | `internal/app/init_catalog.go` | 启动日志可见 capability confirmation load 结果；有持久化记录时加载到 registry 生效 |
| C-4 | [ ] 实现 `factory.go`：New → createStore → NewService → Refresh(启动加载) | `internal/capability/factory.go` | 集成测试：重启后模拟（New + Refresh）可还原以往确认记录到 registry |

## 阶段 D：前端 — 能力图标行

| # | 任务 | 文件 | 验收标准 |
|---|------|------|----------|
| D-1 | [ ] 在 `messages/en.json` + `zh.json` 新增图标相关 i18n key | `web/dashboard/messages/en.json` `zh.json` | 四个能力（chat/embeddings/function_calling/vision）有中文/英文 title 和 aria-label |
| D-2 | [ ] 实现能力图标渲染：从 `row.model?.metadata?.capabilities` + `capability_sources` 计算三级状态，渲染对应图标 | `web/dashboard/src/pages/models/ModelRow.svelte` | — |
| D-2a | [ ] 实测确认态：高亮色 + ✓ 角标（source ∈ {test, observed}） | ModelRow.svelte | 模型有 `capability_sources: {"function_calling":"test"}` → function_calling 图标高亮+✓ |
| D-2b | [ ] 静态声明态：描边半透明（source ∈ {registry, discovered, config, heuristic}） | ModelRow.svelte | 模型有 `capabilities: {"vision":true}` 但 `capability_sources` 无 test/observed → vision 图标描边 |
| D-2c | [ ] 不支持态：不渲染该图标 | ModelRow.svelte | 模型无 `capabilities: {"embeddings":...}` → 嵌入图标不显示 |
| D-3 | [ ] 图标 hover tooltip：显示能力名称 + 来源说明（如"函数调用 · 实测验证""视觉 · 静态推断"） | ModelRow.svelte | hover 会显示正确来源文字 |
| D-4 | [ ] `npm test` 验证图标行测试 | `web/dashboard/tests/` | 新增测试文件或扩展现有模型测试，覆盖三级状态渲染 |

## 阶段 E：前端 — 行内测试结果面板

| # | 任务 | 文件 | 验收标准 |
|---|------|------|----------|
| E-1 | [ ] `modelTest.svelte.js` 新增 `confirm(provider, model)` 方法：筛选 pass 的探针结果 → `PUT /admin/models/capabilities` | `modelTest.svelte.js` | `npm test` 通过；`confirm` 发送正确载荷 |
| E-2 | [ ] ModelRow 行内展开面板：点击"运行能力测试"后，该行下方展开逐 probe 结果列表 | ModelRow.svelte | 点击按钮后展开面板，显示 running/done/failed 态；再次点击收起 |
| E-3 | [ ] 面板渲染逐 probe 结果行：probe 名称 + verdict（✓ pass / △ inconclusive）+ latency + detail（inconclusive 原因） | ModelRow.svelte | 结果列表中每项正确显示 verdict、latency、detail |
| E-4 | [ ] 面板底部「确认能力」按钮：仅所有 probe 完成时可点击；点击后调用 `modelTest.confirm` | ModelRow.svelte | 确认后面板自动关闭；模型列表重新加载（或本地更新）→ 图标变为高亮+✓ |
| E-5 | [ ] 面板显示粗粒度 running 态 + 已耗时计时器 | ModelRow.svelte | running 时面板闪烁「测试中…」+ 秒计数 |

## 阶段 F：前端 — 开关去文字 + 徽标整合

| # | 任务 | 文件 | 验收标准 |
|---|------|------|----------|
| F-1 | [ ] `AccessToggle.svelte` 移除 `<span>{rowToggleLabel}</span>` 文字渲染 | `AccessToggle.svelte:28-29` | 按钮仅显示 track+thumb，无文字；`aria-label` + `title` 保留 |
| F-2 | [ ] 移除/折叠「实测验证 / 流量验证」徽标（其信息已由图标态承载） | ModelRow.svelte | 能力徽标不再渲染；「能力告警 WARN」徽标保留 |
| F-3 | [ ] 更新 i18n 文案：测试按钮 title 增加说明（测试目的、默认探测项、结果查看方式） | `messages/en.json` `zh.json` | 按钮 hover 或 `title` 能清晰说明测试目的 |

## 阶段 G：持久化验证与端到端

| # | 任务 | 文件 | 验收标准 |
|---|------|------|----------|
| G-1 | [ ] 重启加载验证：写 SQLite 确认记录 → 重新 `New` + `Refresh` → 确认 registry 中能力恢复 | `factory_test.go` | 写一条记录后重启（New + Refresh），registry 中对应模型的 `CapabilitySources` 包含正确 source |
| G-2 | [ ] store 表自动创建验证：初次调用时表不存在则自动创建（`CREATE TABLE IF NOT EXISTS`） | `store_sql.go` | 空库首次 Upsert 成功，无错误 |
| G-3 | [ ] 兼容验证：`npm test` 全绿 + `go test ./internal/...` 全绿（受影响 package：admin, capability, providers, app） | Makefile | `make test` 全部通过 |
| G-4 | [ ] 文档同步：三件套文档标记为「已实施」，AD 状态更新 | docs 目录 | 架构设计 AD-1~AD-5 标注实施状态 |

---

## 预估工作量

| 阶段 | 任务数 | 预估工时（h） | 说明 |
|------|--------|---------------|------|
| A | 3 | 1.5 | 仿 pricingoverrides、接口 + SQLite store，已有样板 |
| B | 3 | 2.0 | Service 逻辑 + Refresh/Confirm，mock registry |
| C | 4 | 2.0 | handler 改两写 + init_catalog 启动钩子 + factory |
| D | 4 | 4.0 | 图标行 + 三级态计算 + tooltip + 测试 |
| E | 5 | 5.0 | 行内面板 + 逐 probe 渲染 + confirm 按钮 + 计时器 |
| F | 3 | 1.0 | 去文字 + 徽标整合 + i18n |
| G | 4 | 1.5 | 持久化验证 + auto-migration + make test + doc sync |
| **合计** | **26** | **17.0** | 不含测试bug 修复和 review 迭代 |

执行顺序：A → B → C（后端基础设施）与 D → E → F（前端）可并行；G 为收口验证。