# 工作任务清单：控制台时间窗编辑

> 日期：2026-09-12 ｜ 状态：**全部完成**
> 关联：需求说明.md、架构设计.md（本目录）
> 执行：按阶段顺序 TDD（RED→GREEN→REFACTOR），每阶段完成汇报（测试数、抓出的 bug），待「继续」指令推进下一阶段

---

## 阶段 A：后端数据模型扩展（核心 API）✅

- [x] A-1 `internal/pricingoverrides/types.go`：`Pricing` 结构体新增 `TimeWindows []core.ModelPricingTimeWindow` 字段
- [x] A-2 `internal/pricingoverrides/validation.go`：`validatePricingWindows` 函数（label 非空/utc_ranges 非空/HH:MM 校验/weekday 校验+去重/费率非负）
- [x] A-3 `pricingEmpty`：`len(TimeWindows)==0` 纳入空判断
- [x] A-4 `handler_model_pricing_overrides_test.go`：time_windows upsert/read 往返测试
- **提交**: `f852629b` · 测试: 6 个新 + admin 集成 3 个

## 阶段 B：消费端链路+持久化验证 ✅

- [x] B-1 审计 `usage/extractor.go`、`usage/recalculate_pricing.go`、`usage/group_cache_stats.go`、`usage/savings.go` 全部经过 `ResolvePricing`→`AtTime`
- [x] B-2 `mergePricing` 窗口/标量覆盖逻辑、`DropTimeWindowRatesOverriddenBy` 行为
- [x] B-3 存储往返验证（`store_sql_test.go`：`TestStorePersistenceTimeWindows`）
- **提交**: `481e5ada` · 实战验证: 7 场景 AtTime 求值 (peak/off-peak/weekend/scalar drop)

## 阶段 C：前端——核心编辑逻辑（pricingOverridesLogic.js）✅

- [x] C-1 `buildPricingOverridePayload` 第三参数 `preservedTimeWindows`，深拷贝后并入 payload
- [x] C-2 新增 3 个纯逻辑测试（time_windows payload / 混合 scalar+tw / 深拷贝隔离）
- **提交**: `31ed790c` · 前端测试: 687/687

## 阶段 D：前端——时间窗编辑器 UI ✅

- [x] D-1 `PricingOverrideEditor.svelte` 新增时间窗区域（卡片式布局）
- [x] D-2 窗口卡片：Label 输入 + 7 天 checkbox + HH:MM 时间输入 + 窗口费率 3 字段
- [x] D-3 添加/删除窗口/range 按钮
- [x] D-4 Store 操作方法：`addTimeWindow`/`removeTimeWindow`/`addTimeRange`/`removeTimeRange`/`toggleTimeRangeDay`
- **提交**: `31ed790c` · svelte-check: 0 errors · build: ✅

## 阶段 E：前端——i18n 消息 ✅

- [x] E-1 9 条 `models_tw_*` 消息键（Section/Label/Ranges/Start/End/Add Window/Remove/Add Range/Window rates）
- [x] E-2 中英文消息文件同步（`en.json` / `zh.json`）
- **提交**: `31ed790c` · i18n test: 684/684 → 全部 i18n 键被引用

## 阶段 F：集成测试 ✅

- [x] F-1 API 端到端：curl 集成测试脚本（30/30 通过）
- [x] F-2 错误场景校验（缺 label/bad clock/invalid weekday/empty window rates → 400）
- [x] F-3 标量+时间窗混合覆盖
- [x] F-4 纯时间窗（无标量行）覆盖
- [x] F-5 实战 AtTime 求值验证（peak/off-peak/weekend/scalar drop）

## 阶段 G：文档与复盘 ✅

- [x] G-1 更新 `docs/features/cost-tracking.mdx`（时间窗编辑操作说明 + UI 引用）
- [x] G-2 本任务清单标记完成

---

## 总览

| 阶段 | 状态 | 提交 | 测试 |
|------|------|------|------|
| A 后端数据模型 **后端** | ✅ | `f852629b` | 单元+集成 9 个 |
| B 消费端链路验证 | ✅ | `481e5ada` | 实战验证 7 场景 |
| C 前端编辑逻辑 | ✅ | `31ed790c` | 逻辑测试 3 个 |
| D 前端 UI 组件 | ✅ | `31ed790c` | svelte-check 0errors |
| E i18n 消息 | ✅ | `31ed790c` | i18n test ✅ |
| F 集成测试 | ✅ | — | API 30/30 curl |
| G 文档 | ✅ | 本提交 | — |

### 发现的问题
- macOS 上 curl `-w` 输出到 stdout 而非 stderr（与 Linux 行为不同）
- "tuesday" 等完整英文星期名被 `ParseWeekday` 视为有效（不区分大小写），测试用 "xyz" 替代
- `tests/perf` guard alloc 超限 —— 在 baseline commit 上独立 worktree 验证了完全相同失败，纯环境噪音